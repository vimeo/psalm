build:
  environment:
    postgresql: false
    redis: false

checks:
    php: true

filter:
  excluded_paths:
    - "tests/"
    - "src/Psalm/Stubs/"
    - "examples/"
    - "src/Psalm/CallMap.php"
    - "src/Psalm/PropertyMap.php"
